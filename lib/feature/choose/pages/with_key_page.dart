import 'package:flutter/material.dart';

class WithKeyPage extends StatelessWidget {
  const WithKeyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Deneme'),
    );
  }
}
